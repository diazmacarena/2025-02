#include <iostream>
#include<string>
using namespace std;

template<class T>
struct asc
{ bool operator()(T a, T b)
    { return a<b;}
};

template<class T>
struct des
{ bool operator()(T a, T b)
    { return a>b;}
};

template <class T, class O, int N = 10>
class lista
{ T Lista[N];
int elem = 0;
public:
bool add(T val);
void print();
};

template <class T, class O, int N>
bool listaT, O, N>::add(T val)
{ O comp;
